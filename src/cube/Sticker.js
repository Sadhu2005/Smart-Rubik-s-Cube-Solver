// src/cube/Sticker.js
class Sticker {
    constructor(color, initialFace, initialRow, initialCol) {
        this.color = color;
        this.currentFace = initialFace;
        this.currentRow = initialRow;
        this. currentCol = initialCol;
        this.initialFace = initialFace;
        this.initialRow = initialRow;
        this.initialCol = initialCol;
    }
}

export default Sticker;